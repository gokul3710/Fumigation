#include <stdio.h>
#include <stdlib.h>

int main(void){
    int limit,i,j,numbers[100];
    printf("enter a limit");
    scanf("%d",&limit);
    printf("enter %d numbers",limit);
    for(i=0;i<limit;i++){
        scanf("%d",&numbers[i]);
    }

    for(i=0;i<limit;i++){
        for(j=0;j<limit;j++){
            
        }
    }
}